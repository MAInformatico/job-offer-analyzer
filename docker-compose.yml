services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro

  # Future services:
  # db:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_USER: jobanalyzer
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}
  #     POSTGRES_DB: jobanalyzer
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data: